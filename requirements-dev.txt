# Development & test tooling for the Python backend (apps/api).
# Runtime/production dependencies live in apps/api/requirements.txt.
#
#   pip install -r apps/api/requirements.txt -r requirements-dev.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=8.0.1
flake8>=6.0.0
mypy>=1.5.0
